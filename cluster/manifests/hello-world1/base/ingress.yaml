apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-world1
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-world1
                port:
                  name: http